---
product: openshift_ai
source_urls:
  - https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/latest
best_practices:
  - "Guard driver installs, kernel module changes, and operator upgrades behind `apply_changes` and maintenance windows"
  - "Run inference benchmarks and canary testing after changes to validate performance"
scenarios:
  install:
    preflight:
      - "Confirm cluster prerequisites"
    postinstall:
      - "Apply tuned settings for resource limits and operator configuration when `apply_changes` is true"
  upgrade:
    preflight:
      - "Backup operator CRs and configurations"
    postinstall:
      - "Validate operator health and model-serving workflows"
