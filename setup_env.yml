---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Ensure ~/.ansible/conf/env.yml exists (encrypted if vaultpass present)
      ansible.builtin.command:
        cmd: python3 HealthChecks/tools/env_manager.py --ensure
      changed_when: false
