# idm PreFlight Role

## Title
idm PreFlight HealthCheck Role

## Synopsis
This role runs pre-install and pre-upgrade readiness checks for idm in the HealthChecks PreFlight workflow. It is designed for safe discovery-first execution and only applies changes when explicitly enabled.

## Quickstart
Run this role through the PreFlight runner from /home/sgallego/GIT/HealthChecks:

    cd /home/sgallego/GIT/HealthChecks
    ansible-playbook run_preflight.yml -e "products=['idm']"

Run in apply mode (opt-in):

    cd /home/sgallego/GIT/HealthChecks
    ansible-playbook run_preflight.yml -e "products=['idm'] healthcheck_check_mode_only=false"

## Responsibilities
- Validate host readiness before installation, upgrade, backup, or restore operations
- Check required packages, repositories, mount points, services, and environment prerequisites
- Produce machine-readable artifacts for reporting and automation pipelines
- Keep remediation optional and gated by execution flags

## Execution Model
- Default mode: non-destructive checks and reporting
- Remediation mode: enabled only when healthcheck_check_mode_only=false
- Role-level behavior is controlled by install_deps and apply_changes flags

## Inputs
- Inventory and host access in the active Ansible context
- Environment values from ~/.ansible/conf/env.yml when required
- Product-specific defaults and role variables

## Variables
- No defaults/main.yml found for this role

## Artifacts and Outputs
- JSON readiness output under artifacts_dir for this product
- Shared package-gap and readiness rollups generated by runner playbooks
- Task-level diagnostics captured in role-specific report structures

## File Map
- PreFlight_Healthcheck/roles/idm/tasks/main.yml

## Troubleshooting
- Confirm role secrets and environment inputs are present in ~/.ansible/conf/env.yml
- Validate package manager/repository access from target hosts
- Run syntax checks before broad execution
- Execute a single-role run first to isolate failures

## Validation
    cd /home/sgallego/GIT/HealthChecks
    ansible-playbook --syntax-check run_preflight.yml

## Related
- PreFlight entrypoint: PreFlight_Healthcheck/preflight.yml
- Multi-product runner: run_preflight.yml
- Shared helper: tasks/run_healthcheck_role.yml
