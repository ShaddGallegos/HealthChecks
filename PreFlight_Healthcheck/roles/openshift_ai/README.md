OpenShift AI PreFlight healthcheck role skeleton.

This role contains discovery checks for OpenShift AI prior to install/upgrade.
Tuning and changes must be implemented in `tasks/tuning.yml` and are only included when `apply_changes: true`.
