RHEL Preflight Role
===================

Checks and fixes needed before installing or configuring Red Hat Enterprise Linux based products.

Use with `preflight.yml` by passing `-e "product_name=rhel"`.
